<!DOCTYPE html>
<html>
  <head>
    <title>Coaching Assistant - Admin</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50">
    <nav class="bg-indigo-600 text-white">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between h-16">
          <div class="flex items-center">
            <div class="flex-shrink-0">
              <h1 class="text-xl font-bold">Coaching Assistant</h1>
            </div>
            <div class="hidden md:block">
              <div class="ml-10 flex items-baseline space-x-4">
                <%= link_to "Dashboard", admin_root_path, class: "px-3 py-2 rounded-md text-sm font-medium hover:bg-indigo-700 #{current_page?(admin_root_path) ? 'bg-indigo-700' : ''}" %>
                <%= link_to "Clients", admin_clients_path, class: "px-3 py-2 rounded-md text-sm font-medium hover:bg-indigo-700 #{current_page?(admin_clients_path) ? 'bg-indigo-700' : ''}" %>
                <%= link_to "Send Email", new_admin_one_off_email_path, class: "px-3 py-2 rounded-md text-sm font-medium hover:bg-indigo-700 #{current_page?(new_admin_one_off_email_path) ? 'bg-indigo-700' : ''}" %>
              </div>
            </div>
          </div>
        </div>
      </div>
    </nav>

    <main class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
      <% if flash[:success] %>
        <div class="mb-4 bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative" role="alert">
          <span class="block sm:inline"><%= flash[:success] %></span>
        </div>
      <% end %>

      <% if flash[:error] %>
        <div class="mb-4 bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative" role="alert">
          <span class="block sm:inline"><%= flash[:error] %></span>
        </div>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>
